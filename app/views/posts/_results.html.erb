<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= "#{post.user.name}" %></td>
        <td><%= "#{post.text}" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
